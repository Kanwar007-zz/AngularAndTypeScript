import { AddedDirective } from './added.directive';

describe('AddedDirective', () => {
  it('should create an instance', () => {
    const directive = new AddedDirective();
    expect(directive).toBeTruthy();
  });
});
